export enum Direction {
    Up = 1,
    Down = 2
}

export enum PageType {
    AboutMe = 1,
    Contact = 2,
    Projects = 3,
    None = 4
}

export enum Status {
    Complete = 'Complete',
    InProgress = 'In Progress',
    Incomplete = 'Incomplete',
    Planning = 'Planning',
    Dropped = 'Dropped'
}
