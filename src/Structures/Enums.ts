export enum PageType {
    LandingPage = 1,
    AboutMe = 2,
    Contact = 3,
    Projects = 4
}

export enum Status {
    Complete = 'Complete',
    InProgress = 'In Progress',
    Incomplete = 'Incomplete',
    Planning = 'Planning',
    Dropped = 'Dropped'
}